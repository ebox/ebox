## Process this file with automake to produce Makefile.in

perldir = @PERLPATH@/EBox/Summary

nobase_perl_DATA = Item.pm Module.pm Section.pm Value.pm

MAINTAINERCLEANFILES = Makefile.in

EXTRA_DIST = $(nobase_perl_DATA)
