pre-start script
    DIR=/var/run/clamav
    USER=clamav
    if [ ! -d "$DIR" ]; then
        mkdir -p-m 0755 "$DIR"
        chown "$USER:$USER" "$DIR"
    fi
end script

exec /usr/sbin/clamd
respawn
