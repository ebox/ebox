## Process this file with automake to produce Makefile.in

initddir = /etc/init.d/

initd_SCRIPTS = ebox

bin_SCRIPTS = ebox-test xml-validate-schema eboxmod ebox-tmpl-parse

MAINTAINERCLEANFILES = Makefile.in
