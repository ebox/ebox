## Process this file with automake to produce Makefile.in

Schemadir = $(datadir)/ebox/schemas
Schema_DATA = firewall.xsd types.xsd network.xsd objects.xsd

EXTRA_DIST = $(Schema_DATA)

MAINTAINERCLEANFILES = Makefile.in
